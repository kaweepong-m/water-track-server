{
  "routes": [
    {
      "method": "GET",
      "path": "/water-users",
      "handler": "water-user.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/water-users/count",
      "handler": "water-user.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/water-users/:id",
      "handler": "water-user.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/water-users",
      "handler": "water-user.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/water-users/:id",
      "handler": "water-user.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/water-users/:id",
      "handler": "water-user.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
